# URL base dos testes E2E (Playwright). Sem segredos aqui.
# Vazio ou ausente: cai no default de produ√ß√£o (https://qrdobem.com.br),
# definido em playwright.config.ts.
# Para rodar contra outro ambiente, copie para .env.e2e e ajuste, ou exporte
# a vari√°vel direto no shell antes de rodar os testes.
E2E_BASE_URL=

# Vari·veis para testes E2E de P‚nico (e2e/panic-public.spec.ts)
# Mantenha vazio no .example, mas preencha no seu .env.e2e local para rodar os testes.
E2E_PANIC_CODE_PERSON=
E2E_PANIC_CODE_PET=
E2E_PANIC_CODE_OBJECT=
E2E_PANIC_CODE_NO_SPACE=
E2E_PANIC_CODE_WITH_CONTACTS=

